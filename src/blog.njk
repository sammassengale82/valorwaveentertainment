---
layout: base.njk
title: Blog
---

<h1>Blog</h1>

<ul>
  {% for post in collections.blog %}
    <li>
      <a href="{{ post.url }}">{{ post.data.title }}</a>
      <span>{{ post.date | date("MMMM d, yyyy") }}</span>
    </li>
  {% endfor %}
</ul>